apiVersion: v1
kind: Service

metadata:
  name: atlas-search

spec:
  type: {{ .Values.service.type }}

  selector:
    app: atlas-search

  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}