apiVersion: v1
kind: Service

metadata:
  name: atlas-analytics

spec:
  type: {{ .Values.service.type }}

  selector:
    app: atlas-analytics

  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}