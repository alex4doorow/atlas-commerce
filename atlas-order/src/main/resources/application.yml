server:
  port: ${ATLAS_ORDER_PORT}

spring:
  application:
    name: atlas-order

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${ATLAS_KEYCLOACK_REALM_HOST}

  datasource:
    url: ${ATLAS_ORDER_DATABASE_URL}
    username: ${ATLAS_ORDER_DATABASE_USER}
    password: ${ATLAS_ORDER_DATABASE_PASS}
    hikari:
      pool-name: AtlasOrderHikariPool
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      validation-timeout: 5000

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

  kafka:
    bootstrap-servers: ${ATLAS_KAFKA_URL}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.add.type.headers: false

  jackson:
    default-property-inclusion: non_null

  profiles:
    active: prod

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,beans

clients:
  catalog:
    url: ${ATLAS_CATALOG_URL}

logging:
  level:
    root: INFO
    com.afa.atlas.commerce.order: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE

atlas:
  observability:
    enabled: false


