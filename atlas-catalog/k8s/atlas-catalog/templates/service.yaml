apiVersion: v1
kind: Service

metadata:
  name: atlas-catalog

spec:
  type: {{ .Values.service.type }}

  selector:
    app: atlas-catalog

  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}